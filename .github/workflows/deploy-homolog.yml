name: 🧪 Deploy HOMOLOG

on:
  push:
    branches: ["release/*"]

jobs:
  deploy-homolog:
    runs-on: ubuntu-latest
    environment: homolog

    steps:
      - name: 📦 Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: app-${{ github.sha }}
          path: ./artifacts

      - name: 🧪 Deploy to HOMOLOG
        env:
          HOMOLOG_SERVER: "secrets.HOMOLOG_SERVER"
        run: |
          echo "🧪 Deploying to HOMOLOG..."
          echo "📦 Artifact: app-${{ github.sha }}.zip"
          echo "🌐 Server: ${{ env.HOMOLOG_SERVER }}"
          echo "⏰ Time: $(date)"

          # Simular deploy
          echo "✅ Deploy executado com sucesso para HOMOLOG"
